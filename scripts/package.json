{
  "name": "maritime-news-collector",
  "version": "1.0.0",
  "description": "Automated maritime news collector for Maritime Hub",
  "private": true,
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "rss-parser": "^3.13.0"
  },
  "scripts": {
    "slack:ingest": "node slack-to-datajs.js --limit=30",
    "slack:dry": "node slack-to-datajs.js --dry-run --limit=30"
  }
}